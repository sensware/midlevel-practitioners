# Copy this file to .env (not committed to git) and set MLP_REPO_DIR to the
# absolute path of your clone of this repo. Used by scripts/run_monthly.sh
# so the crontab entry doesn't need to hardcode a machine-specific path.
MLP_REPO_DIR=/path/to/your/clone/of/Midlevel-Practitioners
