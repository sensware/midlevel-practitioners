data/raw/
data/processed/
data/current
logs/
*.pyc
__pycache__/
.env

# latest/ IS tracked in git: it only ever holds small example outputs
# (manifest, summary CSVs, 1000-row sample csv+parquet) -- never the full
# dataset, which stays local-only under data/processed/.
