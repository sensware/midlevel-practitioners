data/raw/
data/processed/
data/current
logs/
*.pyc
__pycache__/

# latest/ is published to the separate `data` branch only (single squashed
# commit per month, via scripts/run_monthly.py) -- never committed on main,
# so main's code history never carries large data blobs.
latest/
